import javafx.application.Application;

/**
 * A Duke.Launcher class to workaround class path issues
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
