public class Command {
    private String cmd;

    public Command(String input) {
        this.cmd = input;
    }

    /**
     * Executes the command string stored in cmd
     */
    public void execute() {
        
    }
}
