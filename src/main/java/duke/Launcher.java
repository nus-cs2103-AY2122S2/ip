package duke;

import javafx.application.Application;

/**
 * Launcher class that serves as entry point into application
 */
public class Launcher {

    /**
     * entry point to Whey GUI
     * @param args arguments for system input
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
