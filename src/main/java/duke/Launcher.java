package duke;

import javafx.application.Application;

public class Launcher {
    /**
     * The main function of the program that launches the application.
     * @param args command lines argument, not expecting to receive any.
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
